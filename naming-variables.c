#include <stdio.h>

int main(void)
{
int number; // I fixed the name of the variable deleting the exclamation sign !
int test_value; // I fixed the name of this variable by changing the space in the word by an underscore
int example1; // I fixed the name of this variable by changing the position of the number 1 after the example
float int_count; // I fixed the this variable by adding a name to the variable for example "variables"
printf("Variables created successfully.\n");
}

